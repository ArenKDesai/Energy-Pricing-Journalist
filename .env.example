# GCP deployment: set this to your GCS bucket name.
# When unset, the collector writes to DATA_DIR (local mode).
GCS_BUCKET=

# Local dev: collector loops every 5 minutes instead of running once.
# Set automatically by docker-compose.yml.
LOCAL_DEV=false

# Local data directory (used when GCS_BUCKET is unset).
DATA_DIR=/data
